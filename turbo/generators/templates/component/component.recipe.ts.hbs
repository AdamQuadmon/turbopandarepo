import type { RecipeConfig } from "@pandacss/types";

const defineRecipe = <T extends RecipeConfig>(config: T) => config;

export const {{camelCase name}}Recipe = defineRecipe({
  name: "{{camelCase name}}",
  jsx: ["{{pascalCase name}}"],
  base: {
    _hover: { },
  },
  compoundVariants: [],
  variants: {
    variant: {
      main: { },
    },
    size: {
      md: { },
    }
  },
});
